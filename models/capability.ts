import {BaseModel} from './base';

export class Capability extends BaseModel {
    static get tableName() {
        return 'capabilities';
    }

    get tableName() {
        return 'capabilities';
    }
}
